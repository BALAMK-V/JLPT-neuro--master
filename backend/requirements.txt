Django>=5.0,<6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
psycopg[binary]>=3.1
python-dotenv>=1.0
django-filter>=24.2
django-storages[s3]>=1.14
boto3>=1.34

# OCR dependencies (installed via Dockerfile system packages + pip)
Pillow>=10.0
pytesseract>=0.3.10
pdf2image>=1.16
# Optional: uncomment for Google Cloud Vision (higher-accuracy Japanese OCR)
# google-cloud-vision>=3.4

# AI question cleaning and vocabulary explanation (set GEMINI_API_KEY in .env to enable)
google-genai>=1.0.0

# Celery task queue for background OCR (set CELERY_BROKER_URL in .env to activate)
celery>=5.3
redis>=5.0

# Django Channels — WebSocket support for real-time multiplayer quiz
channels>=4.0
daphne>=4.0
channels-redis>=4.0

# FSRS-4.5 spaced repetition algorithm (optional; SM-2 is the default)
fsrs>=1.0

# Multi-format flashcard import (Excel/XLSX support)
openpyxl>=3.1

# Security: rate limiting for auth and sensitive endpoints
django-ratelimit>=4.1

